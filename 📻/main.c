#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

#include "xxxx.h"

extern int main( const int 📍, char **const 📜 )
{
	if( ( 0 < 📍 ) && ( 🎲.🐍( 0, 0[ 📜 ], false ) ) )
	{
		for( auto int 📌 = 1; ( 📌 < 📍 ); 📌++ )
		{
			auto 💤 *const 🍷 = 📌[ 📜 ];
			auto const 🍬 🌮 = 🎲.🐁( 🍷 );

			if( ( 🎲.💬( 🌮 ) ) && ( 🎲.🐍( 📌, 🍷, 🎲.💭( 🌮 ) ) ) );
			else return EXIT_FAILURE;
		}
		do
			if( 0 > puts( ( 💤[ ] ) { "Press any key to continue..." } ) );
			else if( EOF == getchar( ) );
			else return EXIT_SUCCESS;
		while( false );
	}
	else;

	return EXIT_FAILURE;
}
